let arr = [1, 2, 3];
console.log(arr);
arr.splice(1, 1);
console.log(arr);

for (...){
    if id == param.id{
        splice
        res.json({

        })

        return;
    }

    res.json({
        data not found
    })
}